還有幾個可以繼續做的方向，按優先順序看：

  目前狀態
  - main 與 origin/main 同步。
  - 目前測試：163 passed，coverage 96%。
  - case_sensitive 與 WARNING 已決定保留但目前不更動、不實作。

  已完成
  ✅ 1. Schema type: float validator（PR #20，2026-05-26）
     新增 FloatValidator，支援 min/max range 驗證。
     src/dataguard/schema/validators/float_validator.py

  ✅ 2. JSON root 結構驗證（PR #22，2026-05-26）
     JSON root 非 array → ParseFailure；array 內非 object 元素 → ParseErrorItem；
     JSONL 每行非 object → ParseErrorItem。

  ✅ 3. 補 clean/edge/ fixtures（PR #24，2026-05-26）
     新增 edge/ 目錄與 3 個空輸入 fixture（CSV/JSON/JSONL）。
     同時修正空內容 ParseFailure bug（json_parser.py）。

  ✅ 4. 補 convert/invalid/ fixtures（PR #25，2026-05-26）
     新增 invalid/ 目錄與 2 個具名 fixture（語法錯誤、object root）。
     將 tmp_path 即時建立的測試改用具名 fixture。

  ✅ 5. 補齊原 backlog 測試缺口
     csv_clean_invalid integration test 已存在。
     boolean schema missing true_values/false_values 測試已存在。
     schema registry unknown type 測試已存在。
     transformer loader FileNotFoundError 測試已存在。

  ✅ 6. 輸出錯誤處理（Issue #27 / PR #28）
     validate 寫 report、clean 寫 output/report、convert 寫 output 失敗時，
     會轉成清楚的 click.ClickException。
     錯誤訊息包含 Failed to write output/report file 與失敗路徑。

  ✅ 7. README transformer 清單同步
      README 已補上 field_map 與 date_format。

  ✅ 8. 補 coverage 小缺口（PR #29，2026-06-02）
      新增 schema loader 異常路徑測試（YAML 解析錯誤、結構驗證錯誤、不支援格式）。
      新增 reporter 未知格式測試。
      新增 transformer 錯誤分支測試（type_cast/dedup/fill_missing 不支援操作）。
      tests/unit/schema/test_loader.py +4 tests
      tests/unit/reporter/test_reporter_init.py +4 tests  
      tests/unit/transformer/test_*.py +6 tests

  保留但目前不更動
  1. case_sensitive
      src/dataguard/schema/models.py 保留 case_sensitive 欄位。
      enum/string validator 目前不使用它，現有大小寫敏感行為不更動。
      README 與 backlog 已註記為 reserved / not currently active。

  2. WARNING level
      src/dataguard/schema/results.py 和 reporter 保留 WARNING / warning_count。
      目前 validators 不產生 warning，warning_count 預期維持 0。
      README 與 backlog 已註記為 reserved / not currently active。

  接下來建議優先做
   - 無明確優先任務，coverage 缺口已完成

  較大的 future work
  - Excel input
  - streaming large files
  - datetime validator
